const chalk = require('chalk') // 字体变颜色
const axios = require('axios') // 数据信息访问
const ora = require('ora') // node中的进度条参数
const configObj = require('../../yconf.json')
function insBlockCover(pacName) {
  console.log(pacName)
}

module.exports = insBlockCover
